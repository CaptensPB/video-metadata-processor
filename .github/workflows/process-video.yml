name: Process Video Metadata

on:
  repository_dispatch:
    types: [process-video]

jobs:
  process-video:
    runs-on: ubuntu-latest
    
    steps:
    - name: Setup
      run: |
        echo "File ID: ${{ github.event.client_payload.file_id }}"
        echo "Tags: ${{ github.event.client_payload.tags }}"
        echo "Success - GitHub Action triggered!"
        
    - name: Return result
      run: |
        echo "Metadata processing complete"
        echo "Ready for n8n integration"
